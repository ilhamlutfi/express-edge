@layout.template({ title: 'Home' })

@slot('main')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header">Home</div>

                <div class="card-body">
                    {{ title }}
                </div>
            </div>
        </div>
    </div>
</div>
@endslot

@end
